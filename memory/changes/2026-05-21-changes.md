# Changes — 2026-05-21

## Summary

Multi-phase production-readiness pass. All 6 phases complete.

---

## Phase 1A — CSS / Design System fixes

- `frontend/src/features/auth/LoginPage.tsx` — replaced glassmorphism class names (`glass-panel`, `input-base`, `brand-button`, `text-brand-700`) with correct Monday Aniston tokens (`floating-card`, `input-field`, `btn btn--primary`, `text-primary`)
- `frontend/src/features/dashboard/DashboardPage.tsx` — same CSS fix pass
- `frontend/src/router/AppRouter.tsx` — `text-brand-600` → `text-primary`, `bg-[var(--base-tint)]` on PageFallback
- `frontend/index.html` — theme-color `#4f46e5` → `#0073ea`; fonts corrected to Poppins + Figtree + JetBrains Mono
- `frontend/vite.config.ts` — PWA theme_color `#4f46e5` → `#0073ea`
- `frontend/src/styles/globals.css` — removed duplicate font `@import` (fonts now only in index.html `<link>`)

## Phase 1B — Backend fixes

- `backend/src/config/env.ts` — `JWT_REFRESH_SECRET` made optional (opaque refresh tokens don't sign JWTs)
- `backend/src/server.ts` — all `console.log/error` replaced with structured `logger.info/error`

## Phase 1C — Dashboard module (was missing, wired in app.ts but not created)

- NEW `backend/src/modules/dashboard/dashboard.service.ts`
- NEW `backend/src/modules/dashboard/dashboard.controller.ts`
- NEW `backend/src/modules/dashboard/dashboard.routes.ts` (route was already in app.ts, now has implementation)

## Phase 1D — PWA icons

- NEW `frontend/public/generate-icons.md` — instructions for generating real brand icons
- NEW `scripts/generate-pwa-icons.js` — canvas-based placeholder generator

## Phase 2 — Capacitor scripts

- `frontend/package.json` — added 7 Capacitor npm scripts (cap:add:android/ios, cap:sync, cap:open, cap:build)

## Phase 3 — Electron workspace

- NEW `agent-desktop/package.json` — workspace with electron ^33, electron-builder ^25, NSIS target
- NEW `agent-desktop/tsconfig.json`
- NEW `agent-desktop/src/main.ts` — BrowserWindow, single-instance lock, tray minimize
- NEW `agent-desktop/src/preload.ts` — contextBridge with named IPC channels
- NEW `agent-desktop/src/ipcHandlers.ts` — file dialog, write (path-guarded), shell.openExternal (protocol-guarded), auto-update install
- NEW `agent-desktop/src/tray.ts` — system tray with Open/Quit menu
- NEW `agent-desktop/src/updater.ts` — electron-updater with silent update check
- NEW `agent-desktop/assets/README.md` — icon asset requirements
- NEW `frontend/src/types/electron.d.ts` — `window.electronAPI` TypeScript types
- `package.json` — added `agent-desktop` to npm workspaces

## Phase 4 — Orphaned stub cleanup

- DELETED `backend/src/services/ai.service.ts` — unreferenced anywhere in the codebase
- `backend/src/jobs/workers/notification.worker.ts` — `console.error` replaced with `logger.error` (structured, with jobId + stack)

## Phase 5 — Token efficiency + new skills + /project-init

- `.claude/hooks/on-prompt.sh` — added 15-word short-circuit (exits with no output for prompts < 15 words); added email and CI/CD dispatch rules
- NEW `.claude/skills/skill-email-patterns.md` — Nodemailer + BullMQ email worker + 3 HTML templates (welcome, password reset, OTP)
- NEW `.claude/skills/skill-ci-cd-patterns.md` — GitHub Actions CI + deploy + Android + Electron release workflows + PM2 ecosystem config
- NEW `.claude/commands/project-init.md` — 10-step command for renaming boilerplate when starting a new project

## Phase 6 — Count sync

- `CLAUDE.md` — added `/project-init` command row; added `skill-email-patterns.md` and `skill-ci-cd-patterns.md` to skills tables
- `memory/INDEX.md` — skills 37→39, commands 16→17
- `memory/project-state.md` — all counts updated, agent-desktop marked done, new skills listed
