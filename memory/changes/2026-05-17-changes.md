# Changes — 2026-05-17

## 20:30 — Claude Code (Opus 4.7)

**Change:** Phase 1 boilerplate scaffold — 99 files created across the full monorepo.
**Files:** see [../project-state.md](../project-state.md) for the complete list.
**Reason:** User prompt requesting full fullstack PWA boilerplate per organization spec.
**Tests added/updated:** None yet — tests deferred to Phase 2.
**Verified by:** File listing inspection (no `npm install` performed yet).

## 22:20 — Claude Code (Opus 4.7)

**Change:** Fixed `backend/tsconfig.json` — switched module/moduleResolution to `NodeNext`, set `noEmit: true` (typecheck only; `tsup` does real build), dropped deprecated `baseUrl` and explicit `types: ["node"]`.
**Files:** `backend/tsconfig.json`
**Reason:** Three errors reported in IDE diagnostics (missing node types, common-source-dir rootDir conflict, baseUrl deprecation).
**Tests added/updated:** None.
**Verified by:** Errors above will clear once `npm install` populates `node_modules` (`@types/node` is in devDependencies).

## 22:25 — Claude Code (Opus 4.7)

**Change:** Built `memory/` system — README, INDEX, project-state, conventions, glossary, plans/, prompts/, changes/, agents/, coordination/, decisions/.
**Files:** `memory/**`
**Reason:** User asked for a persistent memory system supporting multi-agent coordination, plan persistence, fresh-chat context recovery, and cross-tool (Claude Code + Codex) compatibility.
**Tests added/updated:** N/A (documentation/structure).
**Verified by:** Directory listing.
