# Prompts — 2026-05-17

## 20:30 — user

**Prompt summary:** Generate complete fullstack PWA boilerplate per spec (React + Express + Prisma + Capacitor + Electron + AI service + full `.claude/` config).

**Action taken:** Phase 1 scaffold built — 99 files across root, `.claude/`, `shared/`, `backend/`, `frontend/`, `prisma/`. See [../project-state.md](../project-state.md).

## 22:15 — user

**Prompt summary:** Fix red error in `backend/tsconfig.json`; build `store-releases/` for APK/AAB/IPA/EXE; build `memory/` system for multi-agent coordination + plan persistence + change history; add modern UI/UX primitives; make it work with Claude Code AND Codex.

**Action taken (in progress):**
- Fixed `backend/tsconfig.json` — switched to `NodeNext` resolution, added `noEmit` (typecheck-only), dropped deprecated `baseUrl`. Remaining errors will clear once `npm install` runs.
- Building `memory/` system (this file is part of that build).
- Will follow with `.claude/` memory infra, `store-releases/`, UI primitives, and `AGENTS.md` for Codex.
